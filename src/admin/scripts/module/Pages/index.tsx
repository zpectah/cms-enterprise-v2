import React, { useEffect } from 'react';
import { Routes, Route } from 'react-router-dom';

const PagesModule = () => {

	return (
		<>PagesModule (list & detail)</>
	);
};

export default PagesModule;