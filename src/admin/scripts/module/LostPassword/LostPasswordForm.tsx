import React from 'react';

interface LostPasswordFormProps {}

const LostPasswordForm = (props: LostPasswordFormProps) => {
	const {} = props;

	return (
		<>
			<div>...LostPasswordForm...</div>
		</>
	);
};

export default LostPasswordForm;
