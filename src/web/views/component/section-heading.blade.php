<section class="section section--heading">
    <h1 class="title title--page">
        {{$title}}
    </h1>
    @if($description)
        <section class="section">
            <p class="lead">
                {{$description}}
            </p>
        </section>
    @endif
</section>