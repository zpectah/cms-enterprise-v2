<section
    class="section section--comments"
>
    <comments
        assigned="{{$assigned}}"
        assigned-id="{{$assignedId}}"
        email="{{$member['profile']['email']}}"
    >
        Loading
    </comments>
</section>